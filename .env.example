NEXT_PUBLIC_SITE_URL=http://localhost:3000

# Supabase (optional — predictions persist when URL + anon key are set)
NEXT_PUBLIC_SUPABASE_URL=
NEXT_PUBLIC_SUPABASE_ANON_KEY=

# API-Football (https://www.api-football.com/) — World Cup 2026 live scores
API_FOOTBALL_KEY=

# Optional: protect POST /api/world-cup-scores/sync for cron jobs
# CRON_SECRET=
